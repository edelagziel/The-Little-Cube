using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MangeEnemyLevel : MonoBehaviour
{
    // Start is called before the first frame update
    public GameObject[] enemysLevel = new GameObject[10];


    void Start()
    {
       //  MangeEnemyLevel polymorfizm = new enemy();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
